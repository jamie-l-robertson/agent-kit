# Agent kit — ignore hook runtime state
.claude/hooks/state/
.claude/memory/runs/
.tmp-*install*/
.tmp-skills-inv-*/

# Local, per-machine settings (plugin toggles etc) — never ship with the kit
.claude/settings.local.json
